apiVersion: v1
kind: Secret
metadata:
  name: google-cloud-key
type: Opaque
data:
  # INSTRUCTIONS FOR TEAM MEMBERS:
  # 1. Base64 encode your service-account.json:
  #    cat service-account.json | base64 -w 0
  # 2. Paste the result below replacing 'REPLACE_ME'
  # 3. Save as post_consumer-secret.yaml (do not commit that file!)
  key.json: REPLACE_ME
